#pragma once
#include "xtensor/xtensor.hpp"
#include <queue>
#include <functional>

namespace distances{

    template<class PATH_ARRAY>
    void two_sided_pathing_distance(const xt::xexpression<PATH_ARRAY> & paths) {
		
}
